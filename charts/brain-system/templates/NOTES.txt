brain-system has been rendered for namespace {{ .Release.Namespace }}.

Check runtime resources:
  kubectl -n {{ .Release.Namespace }} get deploy,pod,svc,ingress,hpa,cluster,instanceset -o wide

If this is the first install, KubeBlocks may need several minutes to reconcile the database Cluster.
