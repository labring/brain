/**
 * `/workspace` (spec §D.1): the area in the layout replaces the route with
 * the current Workspace's uid once the session names it; nothing to render.
 */
export default function WorkspaceIndexPage() {
  return null;
}
