/** React Flow `type` for AP workload container cards on the project canvas. */
export const CANVAS_CONTAINER_NODE_TYPE = "containerNode" as const;

/** React Flow `type` for DB workload cards on the project canvas. */
export const CANVAS_DATABASE_NODE_TYPE = "databaseNode" as const;

/** React Flow `type` for public access entry cards on the project canvas. */
export const CANVAS_ENTRY_NODE_TYPE = "entryNode" as const;

/** React Flow `type` for Deployment Task placeholder skeletons. */
export const CANVAS_DEPLOYMENT_PLACEHOLDER_NODE_TYPE =
  "deploymentPlaceholderNode" as const;
